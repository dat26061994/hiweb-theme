import themeConfig from 'hiweb-theme-sdk/config';

themeConfig.section('Reviews', 'reviews', section => {

    // Reviews
    section.config('reviews-template').setLabel('Reviews template').setDriver('textarea-input');

});