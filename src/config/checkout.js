import themeConfig from 'hiweb-theme-sdk/config';

themeConfig.section('Checkout', 'Checkout', section => {

    // Reviews
    section.config('checkout-review-template').setLabel('Reviews template').setDriver('textarea-input');
    

});